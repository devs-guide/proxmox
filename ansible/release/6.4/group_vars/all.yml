---
# Release-specific overrides for Proxmox 6.4 (kept minimal; merged in CI)
pages_ansible_dir: "ansible/release/6.4"
package_catalog_filename: "packages.yml"
package_catalog_url: "{{ pages_base_url }}/ansible/debian/{{ package_catalog_filename }}"
apt_cache_valid_time: 0
bootstrap_mode: "bridge_required"
bootstrap_use_raw_only: true
bootstrap_needs_target_python_build: true
