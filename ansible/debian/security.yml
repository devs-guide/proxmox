---
- name: "[debian]: security packages"
  hosts: all
  become: true

  vars:
    ansible_python_interpreter: "{{ ansible_python_interpreter_managed }}"
    security_packages:
      - fail2ban
      - auditd
      - logrotate
      - unattended-upgrades
      - chrony

  tasks:
    - name: Install security and time packages
      command:
        argv: "{{ ['apt-get', '-y', 'install'] + security_packages }}"

    - name: Ensure unattended-upgrades runs daily
      copy:
        dest: /etc/apt/apt.conf.d/20auto-upgrades
        mode: "0644"
        content: |
          APT::Periodic::Update-Package-Lists "1";
          APT::Periodic::Unattended-Upgrade "1";

    - name: Enable chrony service
      service:
        name: chrony
        state: started
        enabled: true
